# Leave unset on Vercel so the frontend uses same-origin /api/v1.
VITE_API_URL=/api/v1

# Optional for local Socket.IO development only.
VITE_SOCKET_URL=http://localhost:5000
VITE_STRIPE_PUBLISHABLE_KEY=
